# Copy to .env.local and adjust as needed.

# Base URL of the FastAPI backend. Only needed in dev — in production the
# frontend build is served by the same FastAPI app, so requests are same-origin.
VITE_API_BASE_URL=http://localhost:8000
